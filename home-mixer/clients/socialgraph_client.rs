/// Social graph client — internal API, excluded from open source release.

/// Client for querying social graph relationships (following, followers, etc.).
pub struct SocialGraphClient;

impl SocialGraphClient {
    pub fn new() -> Self {
        Self
    }
}
